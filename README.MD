# 🤖 Telegram ChatGPT Bot (OpenRouter)

Telegram-бот с поддержкой LLM (ChatGPT-совместимые модели) через **OpenRouter**.
Бот сохраняет контекст диалога, умеет сбрасывать историю и полностью соответствует требованиям учебного задания.

---

## 🚀 Возможности

* Ответы на команды `/start` и `/help`
* Генерация ответов через LLM (OpenRouter)
* Сохранение контекста диалога пользователя
* Сброс контекста:

  * по команде `/start`
  * по кнопке **Новый запрос**
* Хранение токенов и ключей в `.env`

---

## 🛠 Используемые технологии

* Python 3.10+
* aiogram 3
* OpenAI SDK (через OpenRouter)
* python-dotenv

---

## 📁 Структура проекта

```
ChatbotWithLLM/
│── bot.py
│── requirements.txt
│── README.md
│── .gitignore
│── env.example
│── .env (создаётся локально, не хранится в репозитории)
```

---

## ⚙️ Установка и запуск

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/USERNAME/REPOSITORY_NAME.git
cd REPOSITORY_NAME
```

### 2️⃣ Создание и активация виртуального окружения

```bash
python -m venv .venv
.venv\\Scripts\\activate   # Windows
# source .venv/bin/activate  # Linux / macOS
```

### 3️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4️⃣ Настройка переменных окружения

Создайте файл `.env` на основе `env.example` и заполните его своими значениями:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
OPENROUTER_API_KEY=your_openrouter_api_key
```

⚠️ Файл `.env` не должен быть добавлен в GitHub и уже исключён в `.gitignore`.

---

## ▶️ Запуск бота

```bash
python bot.py
```

---
